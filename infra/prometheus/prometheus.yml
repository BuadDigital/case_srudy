global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment when services expose /metrics (OpenTelemetry Prometheus exporter or aspnetcore).
  # - job_name: realestate-api
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ["host.docker.internal:5080"]

  # RabbitMQ: enable plugin in container then add:
  #   rabbitmq-plugins enable rabbitmq_prometheus
  # - job_name: rabbitmq
  #   static_configs:
  #     - targets: ["rabbitmq:15692"]
