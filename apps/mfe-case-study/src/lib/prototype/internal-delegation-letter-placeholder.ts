/** Stub until internal delegation letter is wired from row more menus. */
export function openInternalDelegationLetterPlaceholder(_context: {
  poNumber: string;
  propertyId?: string;
}): void {
  // Intentionally empty — UI hook-up in a later phase.
}
